export interface CulturalPreset {
  id: string;
  label: string;
  flagEmoji: string;
  languageCode: string;
  systemPrompt: string;
  voiceId: string;
  pitchShift: number;
  speedMultiplier: number;
  accentColor: string;
}
